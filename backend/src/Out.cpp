/* ----- HEADER ----- */
#include <stdio.h>

float heap[30101999];
float stack[30101999];
float P;
float H;

/* ------ MAIN ------ */
int main() {
	P = 0;
	H = 0;
	/* ---------- Print ---------- */
	printf("%d", (int) 12);
	printf("%c", (char) 10);
	/* -------- Fin Print -------- */
	/* ---------- Print ---------- */
	printf("%f", (float) 3.14);
	printf("%c", (char) 10);
	/* -------- Fin Print -------- */
	return 0;
}